<!DOCTYPE html>
<html lang="es">
    <head>
        <%- include('../partials/head') %>
    </head>

    <body>
        
        <%- include('../partials/header.ejs') %>
        <div>
            <button class="crear-producto" href="/products/productCreation" type="submit">CREAR NUEVO PRODUCTO</button>
        </div>
        
        <div class="sections-container">
            <section class="bannerinf">
                <div>
                    <img src="/images/resources/bannerinferior.gif">
                </div>
            </section>
            <section>
                <div ><img class="publicidad" src="/images/resources/slider1.png"/></div>
            </section>

            <section>
                <h2 class="titulos titulo-gatos">🙀SECCIÓN GATOS🙀</h2>
                <div class="productos">
                    <% locals.gato && gato.forEach(product => { %>
                        <article class="producto">
                            <img class="producto-img" src="<%= product.image %>" alt="Imagen Producto Perros">
                            <div class="producto-info">
                                <a class="producto-title" href="/products/productDetail/<%= product.id %>"><%= product.name %></a>
                                <p class="price-text"> $ <%= product.price %> </p>
                                <p class="price-text"> Presentación en: <%= product.package %> Kg </p>
                            </div>
                            <div class="producto-buttons">
                                <button href="" data-quantity="1" class="comprar-producto add_cart" rel="nofollow" data-id="<%= product.id %>">Comprar</button>
                                <% if (locals.userLogged) { %>
                                    <% if (locals.userLogged.roles_id === 1) { %>
                                        <a href="/products/<%= product.id %>/productEditor">
                                            <button class="comprar-producto" type="submit">Editar</button>
                                        </a>
                                    <% } %>
                                <% } %>
                            </div>
                        </article>
                    <% }) %>
                </div>
            </section>
        </div>
        <div ><img class="publicidad" src="/images/resources/slider3.png"/></div>
    </section>
        </main>
        <%- include('../partials/footer.ejs') %>
    </body>
</html>